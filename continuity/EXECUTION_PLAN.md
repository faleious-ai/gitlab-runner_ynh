# Runner execution plan v2

The executable authority is the coordinator program: mandate, backlog, state and findings. This file is a human-readable sequence, not a second queue.

1. WP-00/WP-01: architecture and baseline are accepted and hardened.
2. WP-02: repair suite and CI, produce a live Runner/helper-image candidate, validate Linux lifecycle, then add non-promoting automation.
3. WP-08: integrate Runner evidence into cross-repository assurance by exact commit.
4. WP-09: release and maintenance remain governed by program gates.

The Runner consumer filters static tasks and dynamic corrections. The planner defines eligibility, parallelism, checkpoints and completion. Work outside the Runner remains visible in the global plan and cannot be converted into local completion.
