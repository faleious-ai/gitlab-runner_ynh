# Revisão independente — RND-20260716-003

Review-ID: `REV-RND-20260716-003`  
Charter revisado: `CHR-WP01-001`  
Rodada revisora: `RND-20260716-004`  
Revisor: ChatGPT, orquestrador  
Veredito: `ACCEPTED`

## Commit revisado

- baseline: `00db3a0d455db06f54305fef13d6ea596ad6967e`;
- execução: `80489c40e4ea073cd84c7585305ce1cef48cb1ad`;
- coordenador correspondente: `49b592b7e58a5a17d6a4e64d0e2567c95701c955`;
- Round-ID comum: `RND-20260716-003`.

A comparação mostra um único commit e somente arquivos de auditoria, evidência e continuidade. Nenhum manifest, script, action, fixture ou versão foi modificado durante WP-01.

## Outputs conferidos

- `docs/audit/RUNNER_PACKAGE_BASELINE.md`;
- `docs/audit/UPSTREAM_DIVERGENCE.md`;
- `docs/audit/AUTOUPDATE_GAPS.md`;
- `docs/audit/LIFECYCLE_AND_SECURITY_MAP.md`;
- continuidade e evidence index.

## Reproduções do revisor

1. A fixture da fork e do snapshot upstream auditado possuem o mesmo blob SHA e contêm literal com formato de credencial. O valor foi tratado como segredo, não é reproduzido aqui e não foi usado.
2. `actions.json` declara `scripts/actions/register` e esse path não existe.
3. O blob de `actions.json` da fork corresponde ao snapshot upstream auditado.
4. O commit WP-01 não alterou arquivos funcionais.
5. A documentação separa corretamente checks estáticos de runtime YunoHost/Docker/registration não demonstrado.

## Qualidade e limitações

A auditoria cobre manifest, assets, lifecycle, credenciais, Docker, helper images, action, testes, upstream e backlog. Os achados possuem evidência e critérios de próxima prova. Package linter, registro, Docker job, backup/restore e remove continuam `UNVERIFIED`; isso foi declarado, não ocultado.

## Riscos residuais

- credential-like literal presente na árvore atual e histórico público;
- passagem de token em argumento de processo;
- action administrativa sem target;
- backup/restore de identidade não demonstrado;
- `unregister --all-runners` amplo;
- Docker privilegiado e imagem default mutável;
- ausência de updater atômico Runner/helper.

## Gate humano

`HG-RUN-SEC-01`: confirmar revogação, rotação ou expiração do valor histórico no projeto GitLab usado pelo package_check. Não testar a validade usando o valor.

## Decisão

`CHR-WP01-001` está `ACCEPTED`.

A próxima rodada `CHR-WP02-001` está `READY` para remover o risco da árvore atual, prevenir recorrência, reparar o registro e implementar a fundação determinística do updater, sem promover versão nem executar registro real.
