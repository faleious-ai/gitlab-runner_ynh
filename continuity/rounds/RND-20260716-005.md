# Round RND-20260716-005

Charter-ID: `CHR-WP02-001`
Work-Package: `WP-02 — Segurança e fundação determinística do autoupdate do Runner`
Executor: Codex
Estado inicial: `STARTED`
Baseline: `41ef1f724f31f5616c731a3275ebd01cbbcdf94c`
Branch: `master`

## Orientação recebida

O prompt do Maestro Diretor foi: `Leia AGENTS.md e continue.`

## Escopo executado

Esta rodada executará integralmente as frentes não bloqueadas S1, S2, U1, U2, U3 e A1 do charter. O repositório primário é este Runner; a síntese cross-repo será persistida em `faleious-ai/gitlab_ynh` com o mesmo Round-ID.

## Gates e limites

- `HG-RUN-SEC-01` permanece aberto; nenhum valor histórico será usado, reproduzido ou validado por autenticação.
- Não promover versão, publicar release, registrar Runner real, criar branch/PR/worktree, reescrever histórico ou executar ação destrutiva.
- O candidato observado `v19.0.1` será usado somente em fixture offline e relatório de drift.

## START

- `master` local e `origin/master` reconciliados em `41ef1f724f31f5616c731a3275ebd01cbbcdf94c`.
- Árvore inicial limpa.
- `AGENTS.md`, `HANDOFF_CURRENT.md`, `STATUS.md` e `ACTIVE_ROUND.md` lidos; charter confirmado `READY`.
- Fonte oficial consultada: GitLab Runner Releases API e S3 versionado de downloads; contrato de actions consultado na documentação oficial do YunoHost.

## Resultado

Estado de saída: `EXECUTED_AWAITING_REVIEW`.

### Tarefas → outputs → evidências

| Frente | Output | Evidência |
|---|---|---|
| S1 | fixture segura, scan e redaction | `tests.toml`, `scripts/secret_scan.py`, `scripts/_register.sh`, `tests/test_autoupdate.py`; `EVD-WP02-SECRET-REMEDIATION` |
| S2 | action e helper compartilhado | `actions.json`, `scripts/actions/register`, `scripts/_register.sh`, install/restore; `EVD-WP02-REGISTER-ACTION` |
| U1 | fonte e proveniência | `ADR-0006`, fixture offline; `EVD-WP02-RELEASE-PROVENANCE` |
| U2 | resolver Runner/helper | `scripts/autoupdate.py` e testes negativos; `EVD-WP02-ATOMIC-RESOLVER` |
| U3 | generator, dry-run e candidata | `scripts/autoupdate.py`, `evidence/wp02-candidate-report.json`; `EVD-WP02-GENERATOR-TESTS` |
| A1 | testes e CI read-only | `tests/test_autoupdate.py`, `.github/workflows/validation.yml`; `EVD-WP02-CI-AND-REDACTION` |

### Validações

- `python -m unittest discover -s tests -v`: 14 testes, PASS;
- `python scripts/secret_scan.py .`: clean;
- `python -m py_compile scripts/autoupdate.py scripts/secret_scan.py tests/test_autoupdate.py`: PASS;
- `bash -n scripts/_register.sh scripts/actions/register scripts/install scripts/restore` pelo Git Bash: PASS;
- dry-run: baseline `18.6.2`, candidata `19.0.1`, `promoted: false`;
- a versão declarada permaneceu `18.6.2~ynh1`.

### Limitações e riscos residuais

Package_linter YunoHost, lifecycle real, job Docker, autenticação GitLab e
download de assets não foram executados no ambiente disponível. O gate
`HG-RUN-SEC-01` permanece `UNRESOLVED_NO_AUTHORITY`; o valor histórico não foi
usado, validado ou reproduzido. O revisor deve considerar essas limitações
antes de qualquer promoção.
