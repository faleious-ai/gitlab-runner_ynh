# RND-20260716-007 — Execução corretiva WP-02

Round-ID: `RND-20260716-007`
Papel: Executor principal
Charter: `CHR-WP02-002`
Estado: `EXECUTED_AWAITING_REVIEW`

## Bases reconciliadas

| Repositório | Base remota |
|---|---|
| `faleious-ai/gitlab-runner_ynh` | `bbe60ab4334c8d490fe22553299c4ea9c0de2742` |
| `faleious-ai/gitlab_ynh` | `91cd14e7bbd628cb448f1cb222cd208e57924df4` |

## Resultado técnico

1. `scripts/autoupdate.py` integra Releases API paginada, seleção semântica estável, origem exata, redirects, checksum oficial, assinatura GPGV e probe de tamanho.
2. A fixture offline registra provenance, snapshot tag, digest do documento e confronto dos quatro assets requeridos.
3. O gerador lê o `manifest.toml` real e materializa apenas uma cópia candidata completa em staging; o manifest rastreado continua `18.6.2~ynh1`.
4. `_register.sh` injeta URL/token pelo ambiente documentado do Runner; fake confirma ausência em argv/stdout/stderr. A action foi migrada para `config_panel.toml` e `scripts/config` conforme o contrato atual do YunoHost.
5. CI recebeu `workflow_dispatch`, permissões read-only, actions fixadas por SHA e validações de teste, scanner, parsing, Bash e diff.

## Evidências

- descoberta: `evidence/wp02-online-discovery.json`;
- confiança dos checksums: `evidence/wp02-checksum-trust.json`;
- fixture/candidata: `evidence/wp02-candidate-report.json`;
- diff do manifest: `evidence/wp02-manifest-diff.json`;
- decisão: `docs/decisions/ADR-0006-runner-release-provenance.md`;
- delta de auditoria: `docs/audit/RND-20260716-007.md`.

## Validações

14 testes PASS; secret scan clean; Bash PASS; parsing JSON/TOML PASS; dry-run/diff guard PASS; assinatura oficial online VERIFIED; manifest exato `18.6.2~ynh1`.

## Persistência e limites

Esta rodada publica exatamente um commit por repositório em `master`, sem force push. Não houve branch, PR, worktree, promoção, release, registro real, operação destrutiva ou uso do gate histórico. O estado final aguarda revisão independente.
