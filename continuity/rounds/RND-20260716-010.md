# RND-20260716-010 — CHR-WP02-003

Estado inicial: `IN_PROGRESS`
Baseline remoto: `1d0d3bb9e8a1965986f35a73c781f8335c4f9028`
Round-ID: `RND-20260716-010`

## T-WP02D-01-config-controller

Resultado: `LOCAL_VERIFIED`; publicação pendente.

Seam público: `scripts/config` carregado pelo fluxo de configuração YunoHost, com dispatch `run__register()` para `scripts/_register.sh`.

Pre-build challenge: `GO`. Contract reference: `https://github.com/YunoHost-Apps/synapse_ynh/blob/master/config_panel.toml` (upstream YunoHost config-panel examples with `type = "button"` and `bind = "null"`).

- O problema observado é de contrato de interface, não de registro real.
- Os inputs de URL, token e imagem são efêmeros (`bind = "null"`); o token não pode entrar no `argv` nem em logs.
- O controlador é sourceable para o harness e usa o dispatch YunoHost somente quando executado como script.
- Rollback: reverter exclusivamente o commit desta tarefa.

RED observado antes da implementação:

```text
PYTHONDONTWRITEBYTECODE=1 python3 -m unittest tests.test_config_controller -v
KeyError: 'gitlab_url'
AssertionError: unknown GitLab Runner config action
```

GREEN observado após a implementação:

```text
PYTHONDONTWRITEBYTECODE=1 python3 -m unittest tests.test_config_controller -v
Ran 2 tests in ...
OK
```

Gates locais: parser TOML e harness do controlador passaram; token foi efêmero de teste e não foi persistido. A evidência remota será registrada após o commit e novo fetch.

## Revisão interna T01

Spec/Charter: `HOLD` para controller, inputs efêmeros e delegação exclusiva; nenhum P0/P1. O contrato YunoHost observado em fontes upstream usa `run__<id>` e `bind = "null"`; a action legada permanece fora desta tarefa.

Engineering/Security/Lifecycle: `HOLD` para argv/env/redaction no harness; o teste herdado que apontava para o seam obsoleto foi corrigido e retropropagado como `BP-20260716-002`. Lifecycle de instalação/restore não é claim desta tarefa.

Maestro-check: paths alterados pertencem a T01 ou à persistência obrigatória de START/backprop; sem promoção de manifest, registro real, segredo persistido ou operação destrutiva.

## Convergência

| Ciclo | Claims | Gates | P0/P1 | Tarefas | Backprop | Sync | Estado |
|---|---:|---|---|---|---|---|---|
| 1 | 2/2 T01 | focal GREEN | 0 | T01 local | BP-20260716-001 | pendente | CONVERGING |
