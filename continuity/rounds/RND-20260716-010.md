# RND-20260716-010 — CHR-WP02-003

Estado inicial: `IN_PROGRESS`
Baseline remoto: `1d0d3bb9e8a1965986f35a73c781f8335c4f9028`
Round-ID: `RND-20260716-010`

## T-WP02D-01-config-controller

Resultado: `LOCAL_VERIFIED`; publicação pendente.

Seam público: `scripts/config` carregado pelo fluxo de configuração YunoHost, com dispatch `run__register()` para `scripts/_register.sh`.

Pre-build challenge: `GO`. Contract reference: `https://github.com/YunoHost-Apps/synapse_ynh/blob/master/config_panel.toml` (upstream YunoHost config-panel examples with `type = "button"` and `bind = "null"`).

- O problema observado é de contrato de interface, não de registro real.
- Os inputs de URL, token e imagem são efêmeros (`bind = "null"`); o token não pode entrar no `argv` nem em logs.
- O controlador é sourceable para o harness e usa o dispatch YunoHost somente quando executado como script.
- Rollback: reverter exclusivamente o commit desta tarefa.

RED observado antes da implementação:

```text
PYTHONDONTWRITEBYTECODE=1 python3 -m unittest tests.test_config_controller -v
KeyError: 'gitlab_url'
AssertionError: unknown GitLab Runner config action
```

GREEN observado após a implementação:

```text
PYTHONDONTWRITEBYTECODE=1 python3 -m unittest tests.test_config_controller -v
Ran 2 tests in ...
OK
```

Gates locais: parser TOML e harness do controlador passaram; token foi efêmero de teste e não foi persistido. A evidência remota será registrada após o commit e novo fetch.

## Revisão interna T01

Spec/Charter: `HOLD` para controller, inputs efêmeros e delegação exclusiva; nenhum P0/P1. O contrato YunoHost observado em fontes upstream usa `run__<id>` e `bind = "null"`; a action legada permanece fora desta tarefa.

Engineering/Security/Lifecycle: `HOLD` para argv/env/redaction no harness; o teste herdado que apontava para o seam obsoleto foi corrigido e retropropagado como `BP-20260716-002`. Lifecycle de instalação/restore não é claim desta tarefa.

Maestro-check: paths alterados pertencem a T01 ou à persistência obrigatória de START/backprop; sem promoção de manifest, registro real, segredo persistido ou operação destrutiva.

## Convergência

| Ciclo | Claims | Gates | P0/P1 | Tarefas | Backprop | Sync | Estado |
|---|---:|---|---|---|---|---|---|
| 1 | 2/2 T01 | focal GREEN | 0 | T01 local | BP-20260716-001 | pendente | CONVERGING |

## T-WP02D-02-remove-legacy-register

Pre-build challenge: `GO`.

- A interface legada é identificável e não é necessária para o controlador YunoHost publicado em T01.
- O escopo é remoção de entry point, referências ativas e oracles obsoletos; `scripts/_register.sh` permanece a única implementação compartilhada.
- A validação será comportamental no controlador seguro e estrutural apenas para confirmar que o entry point legado não existe mais.
- Nenhuma versão, token, registro real ou lifecycle de produção será tocado.
- Rollback: reverter exclusivamente o commit desta tarefa.

RED observado antes da remoção:

```text
PYTHONDONTWRITEBYTECODE=1 python3 -m unittest tests.test_legacy_register_removed -v
AssertionError: True is not false : legacy scripts/actions/register still exists
```

GREEN observado após a remoção e migração dos oracles:

```text
PYTHONDONTWRITEBYTECODE=1 python3 -m unittest discover -s tests -v
Ran 17 tests in ...
OK
```

Gates locais: `bash -n` dos entrypoints ativos, parser JSON/TOML, `git diff --check` e `python3 scripts/secret_scan.py .` passaram. A workflow de CI deixou de validar o path removido. O teste comportamental continua exercitando `source scripts/config; run__register`, com credenciais somente no ambiente do processo de teste.

## Revisão interna T02

Spec/Charter: `HOLD` para remoção do entrypoint legado, retirada da referência ativa na workflow e migração dos oracles para o controlador YunoHost. Nenhum P0/P1; documentação histórica permanece como evidência histórica, não como interface executável.

Engineering/Security/Lifecycle: `HOLD` para o entrypoint seguro único, ausência de `--token` nos pontos ativos e validação fail-closed antes de execução. Não houve registro real, alteração de versão, persistência de segredo ou operação destrutiva. Lifecycle de backup/restore permanece fora do claim T02.

Maestro-check/guardrails: paths modificados pertencem à tarefa e à evidência de round; não há promoção de manifest nem mudança fora do escopo. O próximo task só inicia após publicação e confirmação de `HEAD == origin/master`.

## Convergência T02

| Ciclo | Claims | Gates | P0/P1 | Tarefas | Backprop | Sync | Estado |
|---|---:|---|---|---|---|---|---|
| 1 | 1/1 T02 | suíte completa + gates estáticos GREEN | 0 | T02 local | 0 | pendente | CONVERGING |

## T-WP02D-03-lifecycle-identity

Pre-build challenge: `GO`.

- O contrato é preservar o diretório de configuração do Runner (`/etc/$app`), incluindo `config.toml` e a identidade gerada pelo próprio Runner, usando os helpers de backup/restore do YunoHost.
- O restore reinstala os pacotes, restaura o diretório e recalcula o checksum, mas não carrega `_register.sh`, não lê `token` e não chama `register_runner_set`; a instalação inicial continua sendo o único registro automático.
- O seam público será a invocação real de `scripts/backup` e `scripts/restore` com helpers, `dpkg`, `yunohost` e Runner falsos em filesystem temporário; o harness verificará conteúdo, permissão, chamadas e ausência de registro.
- Não haverá token real, alteração de versão, serviço ou registro externo. O escopo fica limitado a lifecycle, teste e evidência desta tarefa.
- Rollback: reverter exclusivamente o commit desta tarefa.

RED observado no baseline:

```text
PYTHONDONTWRITEBYTECODE=1 python3 -m unittest tests.test_lifecycle_identity -v
3 falhas: backup sem ynh_backup, restore sem ynh_restore_file e restore ainda chamando register_runner_set.
```

GREEN observado após a implementação:

```text
PYTHONDONTWRITEBYTECODE=1 python3 -m unittest discover -s tests -v
Ran 20 tests in ...
OK
```

Gates locais: harness backup→restore em filesystem temporário, preservação de conteúdo e modo `0600`, ausência de chamada ao Runner falso, `bash -n` dos scripts de lifecycle, parser JSON/TOML, `git diff --check` e `python3 scripts/secret_scan.py .` passaram.

## Revisão interna T03

Spec/Charter: `HOLD` para o contrato `/etc/$app`, uso dos helpers YunoHost, restauração da identidade e registro inicial restrito à instalação. Nenhum P0/P1; upgrade não foi ampliado.

Engineering/Security/Lifecycle: `HOLD` para backup/restore comportamentais; o harness confirma que o arquivo arquivado volta ao destino com modo `0600`, que `restore` não carrega `_register.sh` nem contém a interface de credencial, e que o Runner falso não é executado. O helper YunoHost permanece responsável por ownership/permissões no ambiente real.

Maestro-check/guardrails: paths alterados pertencem a T03, sem registro externo, token real, mudança de release ou ação destrutiva. O próximo task só inicia após publicação e confirmação de `HEAD == origin/master`.

## Convergência T03

| Ciclo | Claims | Gates | P0/P1 | Tarefas | Backprop | Sync | Estado |
|---|---:|---|---|---|---|---|---|
| 1 | 1/1 T03 | suíte completa + harness lifecycle + gates estáticos GREEN | 0 | T03 local | 0 | pendente | CONVERGING |
