# RND-20260717-012 — execução CHR-WP02-004

Round-ID: `RND-20260717-012`
Charter-ID: `CHR-WP02-004`
Work-Package: `WP02E — confiança live, proveniência e fechamento consistente`
Estado: `IN_PROGRESS`
Branch: `master`
Executor: Codex

## START

| Repositório | HEAD remoto no START |
|---|---|
| `faleious-ai/gitlab-runner_ynh` | `53a7a0fd3c1f8e28ff62eb664920c35a4cd20c4c` |
| `faleious-ai/gitlab_ynh` | `b5b3374ad4b18cff568400ed82cf8bcabf024a4f` |

Árvores locais foram reconciliadas com `origin/master` e estão limpas. O charter `CHR-WP02-004` está `READY`; a rodada anterior `RND-20260716-010` permanece `CORRECTION_REQUIRED` conforme `continuity/reviews/REV-RND-20260716-010.md`.

## Pesquisa T-WP02E-01

Questão 1: qual é a origem oficial e o fingerprint da chave usada para verificar os checksums do Runner?

- `VERIFIED_PRIMARY`: a documentação oficial do GitLab Runner aponta `OFFICIAL_KEY_URL` e fingerprint `931D A69C FA3A FEBB C97D AA8C 6C57 C29C 6BA7 5A4E`.
- Fonte: `https://docs.gitlab.com/runner/install/linux-repository/`, observado em 2026-07-17.
- Limitação: a documentação também mostra uma data de expiração que não deve ser convertida em resultado de validade; GPG/GPGV continua sendo a autoridade.

Questão 2: qual cadeia de transporte o endpoint fixado usa atualmente?

- `VERIFIED_PRIMARY`: a abertura do endpoint oficial observou HTTPS e uma resposta final em `d20rj4el6vkp4c.cloudfront.net`, path `/8/56/gpg/runner-gitlab-runner-49F16C5CC3A0F81F.pub.gpg`, com query efêmera não persistida.
- Fonte: endpoint oficial `https://packages.gitlab.com/runner/gitlab-runner/gpgkey/runner-gitlab-runner-49F16C5CC3A0F81F.pub.gpg`, observado em 2026-07-17; a cadeia pode variar por cache/cliente.
- Decisão técnica: aceitar somente o URL inicial exato ou o par host/path CloudFront observado, sempre por HTTPS, com limite de redirects e verificação posterior dos bytes pelo fingerprint/GPGV. Não aceitar wildcard de host/path nem persistir a query.

## T-WP02E-01-official-key-transport

Pre-build challenge: `GO`.

- Seam: `_official_fetch(OFFICIAL_KEY_URL, "key", ...)`, usado por `discover_current()` → `_signature_status()`; o teste confrontará o destino final sanitizado e bytes da cadeia capturada.
- Claims: URL inicial exato; redirect HTTPS bounded; host/path CloudFront explicitamente allowlisted; host/path divergente, IP literal, destino privado e excesso de redirects falham fechado; query efêmera não aparece em diagnóstico/evidência; GPG/GPGV e fingerprint permanecem autoridade.
- Invariantes: nenhum wildcard genérico; nenhuma alteração de manifest; nenhuma chave/credencial persistida; rollback restrito ao commit T01.
- Verificação planejada: teste RED da cadeia oficial capturada, GREEN focal, suíte updater/completa, parsers, `py_compile`, `git diff --check` e `secret_scan`.
- Pesquisa não substitui o probe live de T02; nenhum resultado `verified` será criado nesta tarefa.

RED observado após adicionar o oracle antes da correção:

```text
PYTHONDONTWRITEBYTECODE=1 python3 -m unittest tests.test_autoupdate.AutoupdateTests.test_official_key_transport_accepts_captured_cloudfront_origin tests.test_autoupdate.AutoupdateTests.test_official_key_transport_rejects_unallowlisted_final_origins -v
1 falha: captured CloudFront final origin foi rejeitado por `_official_endpoint(..., "key")`; destinos não allowlisted foram rejeitados corretamente.
```

GREEN observado após a correção:

```text
PYTHONDONTWRITEBYTECODE=1 python3 -m unittest tests.test_autoupdate.AutoupdateTests.test_official_key_transport_accepts_captured_cloudfront_origin tests.test_autoupdate.AutoupdateTests.test_official_key_transport_rejects_unallowlisted_final_origins -v
2 testes passaram.
PYTHONDONTWRITEBYTECODE=1 python3 -m unittest tests.test_autoupdate -v
Ran 24 tests in 0.060s
OK
```

Gates T01: `py_compile` de updater/teste, `git diff --check` e `python3 scripts/secret_scan.py .` (`secret scan: clean`) passaram. Nenhuma observação live `verified` ou alteração do manifest foi produzida.

## Revisão interna T01

Spec/Charter: `HOLD`. O endpoint inicial permanece exato; somente o par CloudFront host/path capturado é aceito; query/fragment, host/path divergente, IP literal e HTTP falham fechado; o limite existente de redirects permanece ativo. O escopo contém apenas updater, teste e round record.

Engineering/Security/Lifecycle: `HOLD`. O validator não aceita wildcard; erros não reproduzem a query efêmera; o fetch padrão valida cada salto e o destino final; o material ainda passa pela conversão GPG/GPGV e fingerprint exato no fluxo existente. Não houve segredo, download persistido, registro, lifecycle ou promoção.

Maestro-check/guardrails: um único Task-ID, dependências T01 independentes, rollback pelo commit T01, fontes primárias registradas, paths remotos/portáveis e níveis de evidência não promovidos. T02 deverá executar o probe live somente após este commit publicado.

## Convergência T01

| Ciclo | Claims | Gates | P0/P1 | Tarefas | Backprop | Estado |
|---|---:|---|---|---|---|---|
| 1 | 1/1 T01 | updater focal + suíte updater + gates estáticos GREEN | 0 | T01 local pronto para sync | 0 | CONVERGING |

## T-WP02E-02-live-trust-observation

Pre-build challenge: `GO`.

- Dependência confirmada: T01 está publicado em `6fb500ec3474c07137fcb8962512ed0adc59a9bb` e `HEAD == origin/master`.
- Seam: CLI público `scripts/autoupdate.py discover`; nenhum mock de transporte ou GPG será usado nesta observação.
- Comando planejado: `python3 scripts/autoupdate.py discover --manifest manifest.toml --report <artefato versionado>`, com timeout/retries padrão; o primeiro resultado será capturado fora da árvore para inspeção e só depois persistido como novo artefato.
- O artefato deverá registrar `producer_commit`, comando, `observed_at`, origem/final host/path sanitizados quando disponíveis, hashes, fingerprint/status e limitação. Query efêmera não será persistida.
- Nenhum pacote completo será baixado, nenhum manifest será promovido e nenhum segredo/registro real será usado. TDD comportamental: `NOT_APPLICABLE`; a prova é a execução pós-commit e o schema/proveniência do artefato novo.

Observação pós-commit:

```text
PYTHONDONTWRITEBYTECODE=1 python3 scripts/autoupdate.py discover --manifest manifest.toml --report evidence/wp02e-live-trust-observation.json --timeout 5 --retries 0
RETURN_CODE=2
autoupdate: source fetch failed after 1 attempts: https://gitlab-runner-downloads.s3.amazonaws.com/v19.2.0/release.sha256
```

O CLI não emitiu o relatório normal porque falhou antes do endpoint da chave e do GPG/GPGV. O resultado foi materializado como o novo artefato `evidence/wp02e-live-trust-observation.json`, ligado ao commit produtor T01, com `result=failed` e trust `not-observed`; não é evidência de assinatura válida nem inválida. O manifest permaneceu `18.6.2~ynh1` e nenhuma credencial/registro foi usado.

RED do contrato de proveniência antes de persistir o artefato:

```text
PYTHONDONTWRITEBYTECODE=1 python3 -m unittest tests.test_live_observation_provenance -v
falha: o novo artefato de observação ainda não existia.
```

GREEN após persistir o resultado observado:

```text
PYTHONDONTWRITEBYTECODE=1 python3 -m unittest tests.test_live_observation_provenance -v
1 teste passou; producer_commit, comando portátil, resultado, estágio, ausência de paths locais/query efêmera e trust não observado conferidos.
```

## T-WP02E-03-historical-evidence-repair

Pre-build challenge: `GO`.

- O seam é a auditoria de provenance dos artefatos JSON e do índice canônico; a implementação funcional do updater não será alterada.
- Os payloads originais serão recuperados do commit remoto T05 `51dbb98a7e6de477c4f3234b1c7d40b4ac1a54ac` e sua integridade será fixada por SHA-256 no teste.
- Nenhum campo factual será adicionado aos snapshots antigos. O índice marcará a observação histórica como `SUPERSEDED`/não corrente, enquanto o artefato T02 será a única fonte da observação live desta rodada.
- RED: o oracle deve detectar que os arquivos atuais divergem dos blobs pré-T06 e que a factualidade não possui supersessão explícita.
- GREEN: os hashes históricos coincidem, o índice registra a supersessão e o novo artefato mantém provenance/resultado próprio.
- Não haverá rede, GPG, download, promoção de manifest, segredo, registro ou alteração de release. Rollback: somente este commit documental.

RED observado antes da restauração/supersessão:

```text
PYTHONDONTWRITEBYTECODE=1 python3 -m unittest tests.test_evidence_portability.EvidencePortabilityTests.test_historical_reports_preserve_pre_t06_semantics_and_are_superseded -v
falha: o índice ainda não possuía a entrada de provenance/supersessão e os artefatos atuais divergiam do estado pré-T06.
```

GREEN após restaurar os payloads e atualizar o índice:

```text
PYTHONDONTWRITEBYTECODE=1 python3 -m unittest tests.test_evidence_portability.EvidencePortabilityTests.test_historical_reports_preserve_pre_t06_semantics_and_are_superseded -v
1 teste passou; SHA-256 dos dois JSONs coincide com os blobs do T05 e a supersessão está registrada.
```

O updater não foi executado nesta tarefa. A correção é documental/provenance-only; o novo resultado live continua exclusivamente no artefato T02, com trust não observado.

## Revisão interna T03

Spec/Charter: `HOLD`. Nenhuma factualidade foi acrescentada aos snapshots; os hashes esperados vieram do commit remoto T05; o índice separa a observação histórica supersedida do novo artefato live falho. O campo antigo `verified` permanece como parte do payload original, não como prova da rodada atual.

Engineering/Security/Lifecycle: `HOLD`. JSONs parseiam, não há paths locais/query efêmera/segredo, e a alteração não toca updater, manifest, registro ou lifecycle. O teste impede nova edição silenciosa dos blobs históricos.

Maestro-check/guardrails: paths limitados a evidence, teste e round record; um único Task-ID; rollback documental; nenhum claim promovido para `REMOTE_CI_VERIFIED` ou `LIFECYCLE_VERIFIED`.

## Convergência T03

| Ciclo | Claims | Gates | P0/P1 | Tarefas | Backprop | Estado |
|---|---:|---|---|---|---|---|
| 1 | 1/1 T03 | provenance focal + hashes históricos + parsers GREEN | 0 | T03 local pronto para sync | 0 | CONVERGING |

## T-WP02E-04-docker-default-consistency

Pre-build challenge: `GO`.

- Seam: parsing público de `config_panel.toml` e `manifest.toml`, com o comportamento de instalação/configuração preservado; não haverá alteração em registro, lifecycle ou no updater.
- Decisão: adotar o default versionado já presente no manifesto (`alpine:3.20`) também no painel. Isso mantém uma única escolha explícita e reproduzível; não será introduzido um default flutuante como `latest`.
- RED: o teste focal deverá falhar enquanto o painel ainda declarar `alpine:latest`, mesmo que o manifesto use `alpine:3.20`.
- GREEN: ambos os contratos TOML declararão `alpine:3.20`, e o teste também confirmará que o controlador continua recebendo a imagem fornecida sem armazenar o token em argv.
- A mudança fica limitada ao painel, teste e registro da rodada. Não haverá download, credencial real, registro de runner, promoção de manifest ou alteração de release.

RED observado antes da correção:

```text
PYTHONDONTWRITEBYTECODE=1 python3 -m unittest tests.test_config_controller.ConfigControllerTests.test_panel_declares_ephemeral_inputs_and_register_button -v
falha: 'alpine:latest' != 'alpine:3.20'
```

GREEN após alinhar o painel:

```text
PYTHONDONTWRITEBYTECODE=1 python3 -m unittest tests.test_config_controller -v
2 testes passaram.
PYTHONDONTWRITEBYTECODE=1 python3 -m unittest discover -s tests -p 'test_*.py'
Ran 35 tests; OK
```

Gates T04: `py_compile`, `python3 scripts/secret_scan.py .` (`secret scan: clean`) e `git diff --check` passaram. O manifesto não foi alterado.

## Revisão interna T04

Spec/Charter: `HOLD`. O painel deixou de usar `alpine:latest` e agora coincide com o default versionado do manifesto (`alpine:3.20`); o teste focal cobre ambos os TOMLs.

Engineering/Security/Lifecycle: `HOLD`. A mudança não toca o controlador compartilhado, transporte de chave, updater, release, registro ou lifecycle; o teste existente continua verificando que o token não aparece em argv.

Maestro-check/guardrails: paths limitados ao painel, teste e round record; um único Task-ID; rollback pelo commit; sem rede, download, segredo, credencial ou claim de CI/lifecycle.

## Convergência T04

| Ciclo | Claims | Gates | P0/P1 | Tarefas | Backprop | Estado |
|---|---:|---|---|---|---|---|
| 1 | 1/1 T04 | config focal + suíte completa + gates estáticos GREEN | 0 | T04 local pronto para sync | 0 | CONVERGING |

## T-WP02E-05-remote-ci-observation

Pre-build challenge: `GO` para observação read-only; `NO_GO` para alterar o workflow, reexecutar jobs, criar status ou promover o nível remoto sem um run associado ao SHA.

- Seam: observação do commit publicado T04 (`2563fc31e1b71db89315fd8c707235ed98659962`) pelos endpoints do conector GitHub para workflow runs e combined status.
- O wrapper de workflow runs filtra execuções acionadas por pull request; status combinado sem entradas não prova execução de `push` em `master`.
- RED: o teste de provenance deverá falhar antes de existir o novo artefato versionado.
- GREEN: o artefato registrará exatamente `workflow_runs=[]`, `statuses=[]`, `gh` ausente e o resultado `UNVERIFIED`, sem misturar os gates locais com CI remoto.
- Não haverá alteração em `.github/workflows/validation.yml`, reexecução, segredo, credencial, registro, download ou promoção de manifest. TDD comportamental: `NOT_APPLICABLE`; a tarefa é read-only/documental.

Observação remota:

```text
github_fetch_commit_workflow_runs(commit=2563fc31e1b71db89315fd8c707235ed98659962)
-> workflow_runs=[] (o wrapper disponível filtra execuções acionadas por pull request)
github_get_commit_combined_status(commit=2563fc31e1b71db89315fd8c707235ed98659962)
-> statuses=[]
command -v gh
-> sem saída; `gh` não está disponível no ambiente
```

O resultado foi materializado em `evidence/wp02e-remote-ci-observation.json` como `UNVERIFIED`. A configuração do workflow continua estruturalmente observada (`push` em `master` e actions fixadas), mas nenhum run/status remoto associado ao SHA foi recuperado. Nenhum run foi inventado ou promovido.

GREEN após persistir o artefato:

```text
PYTHONDONTWRITEBYTECODE=1 python3 -m unittest tests.test_remote_ci_observation -v
1 teste passou.
PYTHONDONTWRITEBYTECODE=1 python3 -m unittest discover -s tests -p 'test_*.py'
Ran 36 tests; OK
```

Gates locais equivalentes: `py_compile`, `secret_scan: clean`, JSON/TOML, Bash, `autoupdate check/generate` offline, manifest inalterado e `git diff --check` passaram. Esses gates não promovem CI remoto.

## Revisão interna T05

Spec/Charter: `HOLD`. O claim remoto permanece `UNVERIFIED` porque não há run/status associado ao SHA; o artefato captura o mecanismo e a limitação exatos, e separa workflow configurado de execução observada.

Engineering/Security/Lifecycle: `HOLD`. A tarefa é read-only/documental; não houve alteração do workflow, reexecução, download, segredo, registro, lifecycle ou promoção de manifest.

Maestro-check/guardrails: paths limitados ao artefato, teste, índice e round record; um único Task-ID; gates locais explicitamente não equivalentes a CI remoto; rollback pelo commit documental.

## Convergência T05

| Ciclo | Claims | Gates | P0/P1 | Tarefas | Backprop | Estado |
|---|---:|---|---|---|---|---|
| 1 | 1/1 T05 | provenance + gates equivalentes locais GREEN; CI remoto não observado | 0 | T05 local pronto para sync | 0 | BLOCKED-ENVIRONMENT |
