# Delta de auditoria — RND-20260716-007

Este documento complementa os baselines históricos de `docs/audit/`; não os reescreve.

## Estado verificado

- a Releases API oficial foi consultada com paginação e selecionou `v19.2.0`;
- os quatro assets necessários foram confrontados com `release.sha256` e a assinatura foi verificada com a chave oficial;
- a fixture `v19.0.1` permanece identificada como snapshot offline;
- o gerador lê o manifest real e cria uma cópia completa em staging, com diff apenas dos nove campos autorizados;
- o manifest rastreado permanece em `18.6.2~ynh1`;
- install, restore e config action compartilham `_register.sh`, com URL/token em ambiente e fora de `argv`;
- a action atual é `main.registration.register` em `config_panel.toml`/`scripts/config`;
- CI tem `workflow_dispatch`, permissões read-only, actions por SHA completo, testes, scanner, parsing, Bash e diff guard.

## Limitações explícitas

Não foi usado token histórico, não houve registro Runner real, não houve operação destrutiva e não houve promoção de release. O lifecycle YunoHost real e o gate `HG-RUN-SEC-01` permanecem fora desta correção.
